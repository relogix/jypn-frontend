export const setPageTitle = (title) => {
  document.title = title ? `${title} - JYPN Fans Website` : "JYPN Fans Website";
};
export const setPageTitleAdmin = (title) => {
  document.title = title ? `${title} - Administrator` : "Administrator";
};
